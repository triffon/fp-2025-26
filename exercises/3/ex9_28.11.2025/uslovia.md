# Упражнение 9 - Списъци, отделяне на списъци и безкрайни списъци

## Задача 1 -  от миналия път
Да се напише функция, която приема предикат и списък и връща най-дългия подсписък изпълняващ предиката

```haskell
ghci> longestSublist (all even) [2, 4, 6, 7, 8, 0, 4, 2, 6, 1]
[8, 0, 4, 2, 6]
```
## Задача 1.1
Да се напише функция, която приема списък и връща най-дългия подсписък, за който първият и последният елемент са еднакви. Най-лесно става с currying.
```haskell
ghci> longestSublist' [2, 4, 6, 7, 8, 0, 4, 2, 6, 1]
[2, 4, 6, 7, 8, 0, 4, 2]
```
## Задача 2
Да се напише функция, която връща кубовете на четните числа в подаден списък
```haskell
ghci> evenCubes [1..10]
[8,64,216,512,1000]
```

## Задача 3 
Да се напише функция, която приема два списъка и връща декартовото произведение на двата списъка (списък от наредени двойки всеки със всеки)
- чрез рекурсия
- чрез отделяне на списъци

Как да променим функцията, че да приема само списъци от числа и да добавим условието сумата на числата да се дели на 3?

```haskell
ghci>  descartes [1, 2, 3, 4] [6, 7, 8, 2]
[(1,6),(1,7),(1,8),(1,2),(2,6),(2,7),(2,8),(2,2),(3,6),(3,7),(3,8),(3,2),(4,6),(4,7),(4,8),(4,2)]
```
## Задача 4
Да се напише функция, която приема стринг с текст и връща най-често срещаната дума (ако са повече от една, няма значение коя). 
*Указание: напишете помощна функция histogram, която връща списък от наредени двойки на думите и тяхната честота на срещане. 
Приемете, че думите се разделят само от точки и празни места, ако ви е трудно делете текста само на празни места.*
```haskell
ghci> mostCommonWord  "Many recursively-defined functions on lists in Haskell show a common pattern of definition. For example, consider the usual definitions of the functions sum (which adds together the numerical elements of a list) and product (which multiples together the numerical elements of a list). These are shown at the top of Figure 1. The similarity between these two functions is made even more apparent if we evaluate them using source reduction. Doing this on the argument [3, 7, 2] is shown below the function definitions in Figure 1. This common pattern of definition is captured by means of the higher-order function foldr of type (a -> b -> b) -> b -> [a] -> b."
"of"
```
# Безкрайни списъци
```haskell
[1..] – Естествените числа
nats :: [Integer]
nats = 0 : map succ nats
ghci> take 5 nats
[0,1,2,3,4]
```

## Задача 5 
Да се дефинира безкраен списък от повторение на дадена стойност
```haskell
ghci> take 8 (repeat' 'a')
"aaaaaaaa"
```
## Задача 6 
Да се дефинира безкраен списък от числата на Фибоначи 
```haskell
ghci> take 5 fib
[0,1,1,2,3]
```

## Задача 7 
Да се дефинира безкраен списък, където n-тия елемент е n!. 

```haskell
ghci> take 6 factList
[1,1,2,6,24,120]
```
## Задача 8 
Да се напише функция, която приема списък и връща списък от всички подмножества. Опитайте се да работи за безкрайни списъци, т.е. да не е нужно да се оценява целият списък наведнъж, за да може да се достъпят първите елементи

## Задача 9 - Бонус, ако ви е скучно 
Да се напише функция, която връща приближение  на e^x чрез формула на Тейлър. Използвайте безкрайни списъци.


